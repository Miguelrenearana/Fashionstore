services:
  - type: web
    name: fashionstore-api
    runtime: docker
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "1440"
      - key: PAYMENT_GATEWAY
        value: mock
      - key: CORS_ORIGINS
        value: https://fashionstore.vercel.app
    healthCheckPath: /health
    dockerfilePath: ./backend/Dockerfile