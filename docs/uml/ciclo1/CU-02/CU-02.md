# CU-02 — Roles y empleados

| CASO DE USO | PROPÓSITO | ACTORES | ACTOR INICIADOR |
| --- | --- | --- | --- |
| CU-02 | Administrar roles y perfiles de empleado: asignar rol, listar roles y vincular empleados a sucursales | Administrador | Administrador |

| PRECONDICIÓN | FLUJO DE SUCESO PRINCIPAL | POSTCONDICIÓN | EXCEPCIÓN |
| --- | --- | --- | --- |
| El administrador tiene sesión válida (JWT) con rol ADMIN. Existen roles registrados en BD. | 1. GET /users/roles lista los roles del sistema. 2. POST /users crea un usuario con uno o más roles. 3. POST /users/{id}/employee vincula datos de empleado (nombres, sucursal, fecha de alta) a un usuario. | El usuario queda asociado a un perfil de empleado y sucursal con su rol asignado. Roles y empleados consultables por el panel admin. | Usuario sin rol ADMIN → 403. Usuario ya vinculado a empleado → 409. Sucursal inexistente → 404. Fecha mal formada → 422. Rol inexistente → 422. |