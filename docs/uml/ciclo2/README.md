# docs/uml/ciclo2/

Diagramas PlantUML (secuencia + comunicación) de los casos de uso del **Ciclo 2**.

Estructura por caso de uso:

```
ciclo2/CU-03/sequence.puml
ciclo2/CU-03/communication.puml
...
```

Plantilla (4 columnas):
`Actor → «UI» → «Controller» → «Model»` (en colaboración: Actor — Boundary — Controller — Entity).
Ver convenciones en RULES.md §6.